## Install dependencies

`composer install`

`npm install`

## Config

copy file .env.example => .env  => config your database

## Combine js, css

`npx run mix`

`npx mix watch`

## Run server

`php artisan serve`

## Migrate database

`php artisan migrate`
